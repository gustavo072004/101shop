<x-app-layout><x-slot name="header"><h1 class="page-title">Marcas</h1><p class="page-subtitle">Registre las marcas que se pueden seleccionar en los productos.</p></x-slot><div class="card-panel mb-5 p-5"><h2 class="mb-4 font-bold">{{ $marca->exists ? 'Editar marca' : 'Agregar marca' }}</h2><form class="flex flex-wrap items-end gap-3" method="POST" action="{{ $marca->exists ? route('marcas.update',$marca) : route('marcas.store') }}">@csrf @if($marca->exists) @method('PUT') @endif<div><label class="label-form" for="nombre">Nombre de la marca</label><input class="input-form" name="nombre" id="nombre" required minlength="2" maxlength="80" value="{{ old('nombre',$marca->nombre) }}">@error('nombre')<p class="error-text">{{ $message }}</p>@enderror</div><button class="btn-primary-101">Guardar marca</button>@if($marca->exists)<a class="btn-secondary-101" href="{{ route('marcas.index') }}">Cancelar</a>@endif</form></div><div class="card-panel overflow-x-auto"><table class="w-full"><thead><tr><th class="table-th">Marca</th><th class="table-th">Estado</th><th class="table-th">Acciones</th></tr></thead><tbody>@forelse($marcas as $item)<tr><td class="table-td">{{ $item->nombre }}</td><td class="table-td">{{ $item->estado ? 'Activa' : 'Inactiva' }}</td><td class="table-td"><div class="flex gap-3"><a class="btn-secondary-101" href="{{ route('marcas.edit',$item) }}">Editar</a><form method="POST" action="{{ $item->estado ? route('marcas.destroy',$item) : route('marcas.reactivar',$item) }}">@csrf @method($item->estado ? 'DELETE' : 'PATCH')<button class="btn-secondary-101">{{ $item->estado ? 'Dar de baja' : 'Reactivar' }}</button></form></div></td></tr>@empty<tr><td colspan="3" class="table-td">Registre una marca para comenzar.</td></tr>@endforelse</tbody></table></div></x-app-layout>
